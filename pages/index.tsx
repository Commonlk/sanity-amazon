import type { NextPage } from 'next';
import Layout from '../components/Layout';

const Home: NextPage = () => {
  return (
    <div>
      <Layout>List Products</Layout>
    </div>
  );
};

export default Home;
